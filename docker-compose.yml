services:
  app:
    image: ${DOCKERHUB_USERNAME:?DOCKERHUB_USERNAME is required}/mohaeng-ai:${IMAGE_TAG:-latest}
    container_name: mohaeng-ai
    env_file:
      - .env
    ports:
      - "8000:8000"
    restart: unless-stopped
